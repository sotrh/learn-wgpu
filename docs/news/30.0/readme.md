# Update to 0.30!

* `SurfaceTexture::present()` -> `Queue::present()`
* `VertexBufferLayout` entries can now be `None`
* `RequestAdapterOptions::apply_limit_bucketing`
* HDR `color_space: wgpu::SurfaceColorSpace::Srgb,`
